// The `SharedWorker` that hands a `MessagePort` from a tab that wants the database to the tab that
// holds it. A `SharedWorker` is identified by its script URL, so this module is the whole of it.
import "weftdb/client/broker-entry";
